Add a banner, shown on the account settings page, warning a user when one of their API tokens is within a configurable number of days of expiring.

Assume this is a real product change with user-visible behaviour. Start the software-development workflow and show what you would do next.
