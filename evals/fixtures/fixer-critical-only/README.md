# Fixer Critical-Only Fixture

Run the focused test:

```sh
ruby test/member_search_test.rb
```

Only the Critical finding should be fixed. Do not rename `MemberSearch`, remove comments, or extract namespaces.
